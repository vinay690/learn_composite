name: 'Run Script'
description: 'Action to run a bash script'

inputs:
  script:
    description: 'The script to run'
    required: true

runs:
  using: 'composite'
  shell: bash
  steps:
    - name: 'Run Script'
      run: |
        ${{ inputs.script }}
